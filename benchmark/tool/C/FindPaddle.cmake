set(PADDLE_ROOT $ENV{PADDLE_ROOT} CACHE PATH "Paddle Path")
find_path(PADDLE_INC NAMES capi.h PATHS ${PADDLE_ROOT}/include/paddle)
find_library(PADDLE_LIB NAMES paddle_capi_shared PATHS ${PADDLE_ROOT}/lib)
if(PADDLE_INC AND PADDLE_LIB)
  message("Found")
else()
  message(${PADDLE_ROOT})
  message("Not Found")
endif()
include_directories(${PADDLE_ROOT}/include)
set(THIRD_PARTY_LIBRARYS)
list(APPEND THIRD_PARTY_LIBRARYS -lglog -lgflags -lprotobuf -lopenblas -lz)
link_directories(${PADDLE_ROOT}/lib)
link_directories(${PADDLE_ROOT}/third_party/gflags/lib)
link_directories(${PADDLE_ROOT}/third_party/glog/lib)
link_directories(${PADDLE_ROOT}/third_party/protobuf/lib)
link_directories(${PADDLE_ROOT}/third_party/openblas/lib)
link_directories(${PADDLE_ROOT}/third_party/zip/lib)
